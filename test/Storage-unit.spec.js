import {
    getOrSetState,
    getState,
    updateState,
    getOrSetBibliography,
    getBibliography,
    resetBibliography,
    updateBibliography
} from '../src/services/Storage';
import chai from 'chai';

chai.should();

describe('Unit tests for state storage', function() {
    it('getOrSetState() should set state if it does not exist', function() {

    });
    it('getOrSetState() should get state if it exists', function() {

    });
});
describe('Unit tests for bibliography storage', function() {
    it('getOrSetBibliography() should create an empty bibliography if it does not exist', function() {

    });
    it('getOrSetBibliography() should get the bibliography object if it exists', function() {

    });
});
